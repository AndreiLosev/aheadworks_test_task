import {sendRequest} from '../api/index';

export class AuthActionsTypee {
    static SIGN_IN = 'SIGN_IN';
}

export class AuthActionCreater {
    static sign_in = (login, password) => dispatch => {
        // dispatch()
    }
}